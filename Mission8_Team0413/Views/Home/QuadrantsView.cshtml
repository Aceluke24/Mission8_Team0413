@{
    ViewData["Title"] = "Quadrants View";
}
<h1>@ViewData["Title"]</h1>

<div class="container mt-4">
    <h2 class="text-center mb-4">Quadrant View</h2>
    
    
    <div class="row">
        <!-- Top Left -->
        <div class="col-md-6 mb-4">
            <div class="card text-center h-100">
                <div class="card-body">
                    @foreach (var a in Model.Where(t => t.QuadrantId == 1 && t.Completed == false))
                    {
                        <p>@a.TaskName</p>
                        <p>@a.DueDate</p>
                        <p>@a.CategoryId.CategoryName</p>
                        <p>@a.Completed</p>
                        <a class="btn btn-info" asp-controller="Home" asp-action="Edit" asp-route-id="@a.TaskId">Edit</a>
                        <a class="btn btn-danger" asp-controller="Home" asp-action="Delete" asp-route-id="@a.TaskId">Delete</a>
                    }
                </div>
            </div>
        </div>

        <!-- Top Right -->
        <div class="col-md-6 mb-4">
            <div class="card text-center h-100">
                <div class="card-body">
                    @foreach (var a in Model.Where(t => t.QuadrantId == 2 && t.Completed == false))
                    {
                        <p>@a.TaskName</p>
                        <p>@a.DueDate</p>
                        <p>@a.CategoryId.CategoryName</p>
                        <p>@a.Completed</p>
                        <a class="btn btn-info" asp-controller="Home" asp-action="Edit" asp-route-id="@a.TaskId">Edit</a>
                        <a class="btn btn-danger" asp-controller="Home" asp-action="Delete" asp-route-id="@a.TaskId">Delete</a>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <!-- Bottom Left -->
        <div class="col-md-6 mb-4">
            <div class="card text-center h-100">
                <div class="card-body">
                    @foreach (var a in Model.Where(t => t.QuadrantId == 3 && t.Completed == false))
                    {
                        <p>@a.TaskName</p>
                        <p>@a.DueDate</p>
                        <p>@a.CategoryId.CategoryName</p>
                        <p>@a.Completed</p>
                        <a class="btn btn-info" asp-controller="Home" asp-action="Edit" asp-route-id="@a.TaskId">Edit</a>
                        <a class="btn btn-danger" asp-controller="Home" asp-action="Delete" asp-route-id="@a.TaskId">Delete</a>
                    }
                </div>
            </div>
        </div>

        <!-- Bottom Right -->
        <div class="col-md-6 mb-4">
            <div class="card text-center h-100">
                <div class="card-body">
                    @foreach (var a in Model.Where(t => t.QuadrantId == 4 && t.Completed == false))
                    {
                        <p>@a.TaskName</p>
                        <p>@a.DueDate</p>
                        <p>@a.CategoryId.CategoryName</p>
                        <p>@a.Completed</p>
                        <a class="btn btn-info" asp-controller="Home" asp-action="Edit" asp-route-id="@a.TaskId">Edit</a>
                        <a class="btn btn-danger" asp-controller="Home" asp-action="Delete" asp-route-id="@a.TaskId">Delete</a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>